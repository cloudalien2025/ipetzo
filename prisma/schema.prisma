generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model AppBootstrapMarker {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  note      String   @default("postgres-foundation")
}
