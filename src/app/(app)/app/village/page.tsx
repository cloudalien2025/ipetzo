import { PlaceholderTabScreen } from "@/components/layout/app-shell-primitives";

export default function VillageShellPage() {
  return (
    <PlaceholderTabScreen
      title="Village"
      description="Collaborator activity, coordination tools, and household workflows are intentionally not built here yet. This screen exists only to establish the shell structure and route destination."
    />
  );
}
