import { PlaceholderTabScreen } from "@/components/layout/app-shell-primitives";

export default function TimelineShellPage() {
  return (
    <PlaceholderTabScreen
      title="Timeline"
      description="Meals, medications, notes, and observations are not implemented in this lane. This placeholder keeps the route, shell rhythm, and bottom-tab experience stable for later work."
    />
  );
}
