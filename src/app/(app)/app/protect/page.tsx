import { PlaceholderTabScreen } from "@/components/layout/app-shell-primitives";

export default function ProtectShellPage() {
  return (
    <PlaceholderTabScreen
      title="Protect"
      description="Plans, alerts, and recovery workflows stay out of scope for this lane. The route is present so the refreshed shell can support future care-protection features without another frame rewrite."
    />
  );
}
